/**
 * Fixture: event-cme-resolved.json
 * Closed record: prediction + nature's answer (hit, −3.2 h signed error).
 */
import fixture from '../../../../contracts/fixtures/events/event-cme-resolved.json';
import type { Event } from '../types/event';
export const eventCmeResolved = fixture as Event;
