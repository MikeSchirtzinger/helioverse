/**
 * Fixture: snapshot-quiet.json
 * Calm conditions, all sources ok, measured delay.
 */
import fixture from '../../../../contracts/fixtures/snapshot/snapshot-quiet.json';
import type { Snapshot } from '../types/snapshot';
export const snapshotQuiet = fixture as Snapshot;
