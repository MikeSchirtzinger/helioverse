/**
 * Fixture: event-flr.json
 * The linked flare; shows the causes/caused_by graph edges.
 */
import fixture from '../../../../contracts/fixtures/events/event-flr.json';
import type { Event } from '../types/event';
export const eventFlr = fixture as Event;
