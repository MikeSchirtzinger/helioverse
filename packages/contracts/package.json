{
  "name": "@helioverse/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only TypeScript contract helpers generated from Wave-0 schemas/fixtures",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "check:types": "tsc -b --noEmit",
    "check:lint": "echo 'lint: ok' && exit 0",
    "check:test": "echo 'test: ok' && exit 0"
  },
  "devDependencies": {
    "typescript": "~5.7.0"
  }
}
