[package]
name = "helio-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Helioverse shared physics/scoring crate (WASM target)"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[dev-dependencies]
