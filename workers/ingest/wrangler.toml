# helio-ingest — Cloudflare Worker: Ingest + Snapshot Writer
name = "helio-ingest"
main = "build/worker/shim.mjs"
compatibility_date = "2025-01-01"

# Placeholder — fill in your account_id for live deployment.
# account_id = ""

# R2 bucket bindings (created via wrangler or dashboard)
# [[r2_buckets]]
# binding = "PUBLIC_BUCKET"
# bucket_name = "helioverse-public"

# KV namespace for snapshot/latest (Worker-internal reads)
# [[kv_namespaces]]
# binding = "KV"
# id = ""

[env.production]
# workers_dev = false
# route = "..."
